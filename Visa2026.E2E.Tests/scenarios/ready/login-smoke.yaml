# Ready scenario — map: login-smoke_map.md (promoted scenarios/ready/)

id: login-smoke
e2eId: E2E-001
description: Log on as standarduser and assert authenticated app shell
user: standarduser
password: ""

steps:
  - login:
      user: standarduser
      password: ""
  - assert-shell: true
