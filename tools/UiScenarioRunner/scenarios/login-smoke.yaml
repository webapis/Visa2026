# Ready scenario — map: login-smoke_map.md
# Hooks: docs/UI_TEST_HOOKS.md

id: login-smoke
description: Fill logon fields and submit (Admin)
requiresAuth: false

steps:
  - goto: /LoginPage
  - fill:
      login-user-name: Admin
      login-password: ""
  - click: login-submit
