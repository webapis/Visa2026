# Windows Docker Desktop — helper files

Copy into `C:\visa2026\` when enabling HTTPS:

- `docker-compose.https.override.yml`
- `Caddyfile` (from `Caddyfile.example`)

Canonical steps: [../ON_PREM_WINDOWS_DOCKER_DESKTOP.md](../ON_PREM_WINDOWS_DOCKER_DESKTOP.md) (HTTPS section).