# Issued visa origin — Mermaid diagram sources

Canonical flowcharts for [`docs/APPLICATION_PROFILE_ISSUED_VISA_ORIGIN.md`](../../APPLICATION_PROFILE_ISSUED_VISA_ORIGIN.md).

| File | Purpose |
|------|---------|
| `mental-model-combined.mmd` | Both profile families (overview) |
| `extension-profile.mmd` | Extension / direct visa (`App_Visa_Ext`, …) |
| `invitation-profile.mmd` | Invitation-based visa + **Issue visa** shortcut |
| `invitation-item-issued-vs-input.mmd` | Issued output lines vs input M2M; only issued lines → `Visa.IssuingInvitationItem` |

The canonical doc embeds the same diagrams as fenced ` ```mermaid ` blocks. **Edit the `.mmd` files first**, then sync the matching blocks in `APPLICATION_PROFILE_ISSUED_VISA_ORIGIN.md`.

Preview: open a `.mmd` in Cursor/VS Code with a Mermaid preview extension, or paste into [mermaid.live](https://mermaid.live).
