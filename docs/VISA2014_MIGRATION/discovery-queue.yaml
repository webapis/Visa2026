# Discovery bootstrap pointer
#
# BO discovery order is NOT listed here — use the canonical dependency-ordered list:
#   Visa2026.DataImporter/legacy/visa2014/order.yaml  →  entities[]
#
# That same file drives OData import order. Keep one ordered list; do not maintain a separate queue.

version: 1
sourceOfTruth: Visa2026.DataImporter/legacy/visa2014/order.yaml

bootstrapOnce:
  - id: global-schema-index
    description: Table list + row counts; identify XAF/system tables to skip
    artifact: docs/VISA2014_MIGRATION/schema-snapshot.md
    status: not_started
    note: Mirror status in order.yaml bootstrapOnce when complete
